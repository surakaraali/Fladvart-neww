import { ReactNode } from 'react';

export const metadata = {
  title: 'Giriş Yap - FLADVart',
  description: 'FLADVart Admin Panel Girişi',
};

export default function AuthLayout({ children }: { children: ReactNode }) {
  return children;
}
